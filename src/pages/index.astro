---
import Layout from '../layouts/Layout.astro';
import { SITE_CONFIG } from '../config/site';
---

<Layout title="Farm Eolica | Organic Lab - Living with Wind, Evolving with Data">
  <!-- Hero Section -->
  <section class="relative h-screen w-full overflow-hidden">
    <!-- Background Image -->
    <div class="absolute inset-0">
      <div
        class="w-full h-full bg-gradient-to-br from-warm-gray-100 via-stone-white to-warm-gray-200"
        style="background-image: url('/images/top/hero.jpg'); background-size: cover; background-position: center;"
      >
        <!-- Overlay with eolica-green tint -->
        <div class="absolute inset-0 bg-eolica-green/30"></div>
      </div>
    </div>

    <!-- Hero Content -->
    <div class="relative h-full flex items-center justify-center">
      <div class="text-center text-white px-6 max-w-4xl">
        <p class="font-mono text-xs tracking-[0.3em] mb-4 opacity-80">
          [ ORGANIC LABORATORY ]
        </p>
        <h1 class="font-serif text-6xl md:text-8xl tracking-wide mb-6 animate-fade-in">
          Farm Eolica
        </h1>
        <p class="text-lg md:text-xl tracking-wide font-light opacity-90 font-serif italic" style="animation-delay: 0.3s;">
          Living with Wind, Evolving with Data.
        </p>
        <p class="text-sm md:text-base tracking-wider font-serif opacity-70 mt-3" style="animation-delay: 0.5s;">
          - 風と共に生き、データと共に進化する。-
        </p>
      </div>
    </div>

    <!-- Scroll Indicator -->
    <div class="absolute bottom-12 left-1/2 transform -translate-x-1/2">
      <div class="font-mono text-xs text-white opacity-60 flex flex-col items-center gap-2">
        <span>SCROLL</span>
        <svg
          class="w-5 h-5 animate-bounce"
          fill="none"
          stroke="currentColor"
          viewBox="0 0 24 24"
        >
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            stroke-width="1.5"
            d="M19 14l-7 7m0 0l-7-7m7 7V3"
          ></path>
        </svg>
      </div>
    </div>
  </section>

  <!-- Prologue Section -->
  <section class="py-32 md:py-40 bg-stone-white">
    <div class="max-w-7xl mx-auto px-6 lg:px-12">
      <div class="grid md:grid-cols-2 gap-12 lg:gap-16 items-center">
        <!-- Left: Text -->
        <div>
          <div class="space-y-12 text-left font-serif text-wet-soil tracking-widest leading-loose animate-fade-in-up">
            <p class="text-lg md:text-xl">
              Farm Eolicaは、<br class="hidden md:inline">
              福島県浪江町にある小さな農場です。
            </p>

            <p class="text-lg md:text-xl">
              季節の花を育てるのは、<br class="hidden md:inline">
              わたし一人と、家族の手伝い。
            </p>

            <p class="text-lg md:text-xl">
              風と土と科学が共存する、自然の中のアトリエ。
            </p>

            <p class="text-lg md:text-xl">
              自然の声に耳を傾け、データと共に進化する。
            </p>

            <p class="text-lg md:text-xl">
              そんな私たちの「研究成果」を、<br class="hidden md:inline">
              どうぞご覧ください。
            </p>
          </div>

          <!-- Signature (右寄せ) -->
          <div class="flex justify-end pt-12 mt-12 !bg-transparent">
            <img src="/images/top/signature.png" alt="Signature" class="h-16 opacity-70 !bg-transparent mix-blend-multiply" loading="lazy" />
          </div>

          <!-- CTA (左寄せ) -->
          <div class="pt-8">
            <a
              href="/about"
              class="inline-block px-12 py-4 text-xs tracking-widest uppercase font-mono border-2 border-old-copper text-old-copper hover:bg-old-copper hover:text-white transition-all duration-300"
            >
              Read Our Story
            </a>
          </div>
        </div>

        <!-- Right: Images (2枚を重ねる) -->
        <div class="relative h-[500px] md:h-[600px]">
          <!-- 画像1: 奥 -->
          <img
            src="/images/top/prologue_1.jpg"
            alt="Farm Eolica"
            class="absolute top-0 left-0 w-4/5 h-3/5 object-cover shadow-lab animate-slide-in-right opacity-0"
            loading="lazy"
            style="animation-delay: 0.2s;"
          />

          <!-- 画像2: 手前、少しずらす -->
          <img
            src="/images/top/prologue_2.jpg"
            alt="Farm Eolica"
            class="absolute bottom-0 right-0 w-4/5 h-3/5 object-cover shadow-lab animate-slide-in-right opacity-0"
            loading="lazy"
            style="animation-delay: 0.4s;"
          />
        </div>
      </div>
    </div>
  </section>

  <!-- Spacer -->
  <div class="h-32 md:h-48"></div>

  <!-- Section 01: PRODUCTS (ATELIER) -->
  <section class="py-20 md:py-28 overflow-hidden" data-animate>
    <div class="max-w-7xl mx-auto px-6 lg:px-12">
      <div class="grid md:grid-cols-2 gap-8 lg:gap-12 items-center">
        <!-- Left: Image (横長) -->
        <div class="aspect-[3/2] md:aspect-[16/10]" data-animate="image-wind">
          <div class="w-full h-full bg-gradient-to-br from-warm-gray-200 to-warm-gray-100 overflow-hidden shadow-lab">
            <img
              src="/images/top/atelier.jpg"
              alt="Atelier - Products & Works"
              class="w-full h-full object-cover"
              loading="lazy"
            />
          </div>
        </div>

        <!-- Right: Text -->
        <div class="space-y-6">
          <p class="font-mono text-xs tracking-widest text-warm-gray-500">
            01 — ATELIER
          </p>
          <h2 class="font-serif text-3xl md:text-4xl tracking-wide text-eolica-green">
            Products & Works
          </h2>
          <div class="space-y-4 text-wet-soil leading-relaxed">
            <p>
              規格外の花、ドライフラワー、キャンドル。<br />
              実験室から生まれる、すべての価値をここに。
            </p>
          </div>
          <a
            href="/products"
            class="inline-block mt-6 text-xs tracking-widest uppercase font-mono border-b-2 border-old-copper pb-1 hover:border-eolica-green transition-colors duration-300"
          >
            Visit Atelier →
          </a>
        </div>
      </div>
    </div>
  </section>

  <!-- Spacer -->
  <div class="h-32 md:h-48"></div>

  <!-- Section 02: CULTIVATION (FORECAST) - Full Width Background -->
  <section class="relative py-24 md:py-32 overflow-hidden" data-animate>
    <!-- Background Image with Overlay -->
    <div class="absolute inset-0 z-0">
      <img
        src="/images/top/forecast.jpg"
        alt="Cultivation Background"
        class="w-full h-full object-cover"
        loading="lazy"
      />
      <!-- Dark Overlay for Text Readability -->
      <div class="absolute inset-0 bg-wet-soil/85"></div>
    </div>

    <!-- Content -->
    <div class="relative z-10 max-w-5xl mx-auto px-6 lg:px-12">
      <!-- Section Label -->
      <div class="mb-12">
        <p class="font-mono text-xs tracking-widest text-stone-white/70">
          02 — FORECAST
        </p>
      </div>

      <!-- Dashboard Style Content -->
      <div class="space-y-8">
        <h2 class="font-serif text-3xl md:text-5xl tracking-wide text-white leading-tight">
          Shipment Log
        </h2>
        <div class="space-y-4 text-stone-white/90 leading-relaxed text-base md:text-lg">
          <p>
            市場・生花店様へ。<br />
            現在の生育状況と出荷予報をリアルタイムで公開しています。
          </p>
          <p>
            温度、湿度、日照時間。<br />
            データに裏打ちされた栽培で、最高の品質を目指しています。
          </p>
        </div>

        <!-- Data Points (Dashboard Style) -->
        <div class="grid grid-cols-3 gap-6 pt-8 border-t border-stone-white/30">
          <div>
            <p class="font-mono text-2xl md:text-3xl text-old-copper font-bold">24/7</p>
            <p class="font-mono text-xs text-stone-white/70 mt-1">Monitoring</p>
          </div>
          <div>
            <p class="font-mono text-2xl md:text-3xl text-old-copper font-bold">IoT</p>
            <p class="font-mono text-xs text-stone-white/70 mt-1">Sensors</p>
          </div>
          <div>
            <p class="font-mono text-2xl md:text-3xl text-old-copper font-bold">B2B</p>
            <p class="font-mono text-xs text-stone-white/70 mt-1">Forecast</p>
          </div>
        </div>

        <a
          href="/cultivation"
          class="inline-block mt-8 text-xs tracking-widest uppercase font-mono border-b-2 border-old-copper text-old-copper pb-1 hover:border-stone-white hover:text-stone-white transition-colors duration-300"
        >
          Check Forecast →
        </a>
      </div>
    </div>
  </section>

  <!-- Spacer -->
  <div class="h-32 md:h-48"></div>

  <!-- Section 03: DIALOGUE -->
  <section class="py-32 md:py-40" data-animate>
    <div class="max-w-7xl mx-auto px-6 lg:px-12">
      <div class="grid md:grid-cols-12 gap-12 items-center">
        <!-- Image (Wide, Landscape) -->
        <div class="md:col-span-8 order-2 md:order-1" data-animate="image-wind">
          <div class="aspect-[4/3] bg-gradient-to-br from-warm-gray-200 to-warm-gray-100 overflow-hidden">
            <img
              src="/images/top/dialogue.jpg"
              alt="Dialogue with Customers"
              class="w-full h-full object-cover"
              loading="lazy"
            />
          </div>
        </div>

        <!-- Text (Compact Box) -->
        <div class="md:col-span-4 order-1 md:order-2 space-y-6">
          <p class="font-mono text-xs tracking-widest text-warm-gray-500">
            03 — DIALOGUE
          </p>
          <h2 class="font-serif text-3xl md:text-4xl tracking-wide text-eolica-green">
            Grown<br />with You
          </h2>
          <div class="text-wet-soil leading-relaxed">
            <p>
              「こんな花が欲しい」<br />
              「この色をもっと濃く」
            </p>
            <p class="mt-4">
              あなたの声が、<br />
              次の栽培計画を作ります。
            </p>
            <p class="mt-4">
              農家と消費者が対話しながら育てる、<br />
              新しい農業のかたち。
            </p>
          </div>
          <a
            href="/contact"
            class="inline-block mt-8 text-xs tracking-widest uppercase font-mono border-b-2 border-old-copper pb-1 hover:border-eolica-green transition-colors duration-300"
          >
            Join Request →
          </a>
        </div>
      </div>
    </div>
  </section>

  <!-- Spacer -->
  <div class="h-32 md:h-48"></div>

  <!-- Instagram Section: Updates from the Lab -->
  <section class="py-32 md:py-40 bg-warm-gray-50/50" data-animate>
    <div class="max-w-4xl mx-auto px-6 lg:px-12">
      <!-- Section Header -->
      <div class="text-center mb-16">
        <p class="font-mono text-xs tracking-widest text-warm-gray-500 mb-4">
          INSTAGRAM
        </p>
        <h2 class="font-serif text-3xl md:text-4xl tracking-wide text-eolica-green mb-6">
          Updates from the Lab
        </h2>
        <p class="text-wet-soil leading-relaxed max-w-2xl mx-auto">
          日々の栽培記録、花の成長、ハウスの様子をInstagramで発信しています。
        </p>
      </div>

      <!-- Instagram Grid (3x3) - Lab Style -->
      <div class="grid grid-cols-3 gap-2 md:gap-4 mb-12">
        <!-- Instagram画像: /images/instagram/01.jpg ~ 09.jpg -->
        {[1, 2, 3, 4, 5, 6, 7, 8, 9].map((num) => (
          <div class="aspect-square bg-warm-gray-200 overflow-hidden">
            <img
              src={`/images/instagram/${String(num).padStart(2, '0')}.jpg`}
              alt={`Instagram Post ${num}`}
              class="w-full h-full object-cover grayscale hover:grayscale-0 hover:scale-105 transition-all duration-500"
              loading="lazy"
            />
          </div>
        ))}
      </div>

      <!-- Follow Button -->
      <div class="text-center">
        <a
          href={SITE_CONFIG.sns.instagram}
          target="_blank"
          rel="noopener noreferrer"
          class="inline-block px-12 py-4 text-xs tracking-widest uppercase font-mono border-2 border-old-copper text-old-copper hover:bg-old-copper hover:text-white transition-all duration-300"
        >
          Follow on Instagram
        </a>
      </div>
    </div>
  </section>

  <!-- Closing Section -->
  <section class="py-40 md:py-52 bg-eolica-green text-white" data-animate>
    <div class="max-w-4xl mx-auto text-center px-6">
      <p class="font-mono text-xs tracking-[0.3em] mb-8 opacity-60">
        — ORGANIC LABORATORY —
      </p>
      <p class="font-serif text-3xl md:text-4xl tracking-wide leading-relaxed">
        花と、データと、物語を。
      </p>
      <p class="mt-8 text-lg opacity-80 font-serif">
        Farm Eolicaは、新しい農業の可能性を探求しています。
      </p>
    </div>
  </section>
</Layout>

<style>
  @keyframes fade-in {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  /* Prologue: シンプルな下から浮き出るアニメーション */
  @keyframes fade-in-up {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .animate-fade-in-up {
    animation: fade-in-up 1.2s ease-out forwards;
  }

  /* 風の演出: ふわっと浮遊するアニメーション */
  @keyframes wind-float {
    from {
      opacity: 0;
      transform: translateY(60px) scale(0.98);
      filter: blur(2px);
    }
    to {
      opacity: 1;
      transform: translateY(0) scale(1);
      filter: blur(0);
    }
  }

  /* 風の演出: 画像が風で揺らぐように現れる */
  @keyframes wind-sway {
    0% {
      opacity: 0;
      transform: translateX(-30px) translateY(30px) rotate(-2deg) scale(0.95);
    }
    60% {
      transform: translateX(5px) translateY(-5px) rotate(0.5deg) scale(1.01);
    }
    100% {
      opacity: 1;
      transform: translateX(0) translateY(0) rotate(0) scale(1);
    }
  }

  .animate-fade-in {
    animation: fade-in 1.5s ease-out forwards;
  }

  /* 画像の風アニメーション */
  [data-animate="image-wind"] {
    opacity: 0;
    transition: opacity 1.5s ease-out;
  }

  [data-animate="image-wind"].animate-in {
    opacity: 1;
    animation: wind-sway 1.8s cubic-bezier(0.34, 1.56, 0.64, 1) forwards;
  }

  /* Fallback for images */
  img[src^="/images/top/"],
  img[src^="/images/instagram/"] {
    background: linear-gradient(135deg, #D5D3CF 0%, #B8B6B2 100%);
  }
</style>
